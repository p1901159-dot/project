# project
# -*- coding: utf-8 -*-
"""PROJECT(INTERNSHIP).ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1PFDeAsu7zBaE4H0KUJCpbywLE_Aw_Eu8
"""

!pip install tensorflow opencv-python matplotlib scikit-learn

import cv2
import numpy as np
import matplotlib.pyplot as plt

from sklearn.cluster import KMeans

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Conv2D

image = cv2.imread("/content/sacred-lotus-flower-67ab6a40aef25.jpg.avif")

image = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

plt.imshow(image)
plt.axis("off")
plt.show()

model = Sequential()

model.add(Conv2D(16,
                 (3,3),
                 activation='relu',
                 padding='same',
                 input_shape=(128,128,3)))

model.add(Conv2D(32,
                 (3,3),
                 activation='relu',
                 padding='same'))

model.summary()

img = cv2.resize(image,(128,128))

img = img/255.0

img = np.expand_dims(img,axis=0)

features = model.predict(img)

print(features.shape)

pixels = image.reshape((-1,3))

k = 5

kmeans = KMeans(n_clusters=k, random_state=42)

kmeans.fit(pixels)

colors = kmeans.cluster_centers_

labels = kmeans.labels_

plt.figure(figsize=(10,2))

for i,color in enumerate(colors):

    plt.subplot(1,k,i+1)

    plt.imshow([[color.astype("uint8")]])

    plt.axis("off")

plt.show()

print("Dominant Colors (RGB):")

for i,color in enumerate(colors):

    print(f"Color {i+1}: {color.astype(int)}")
